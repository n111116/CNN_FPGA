# box和crop回退到4月1日的版本（显示有一定bug）
此时闪烁问题基本消失
# 问题1：特别大的图 因为检测结果在最后一行之前就出来了 所以会显示下半部分后直接被清除
box加入valid保护
*此时字符出现明显闪烁 （疑似闪烁的根源）*
*已解决* *对end_crop_wr相关部分也加入保护后 闪烁消失* 
# 问题2：crop中写入和读出速度不匹配的踩踏问题，需要上锁主动丢帧
写入速度取决于一张图内有几帧，而读出速度受制于lprnet本身的计算速度
*已解决*
# 问题3：图里有8个子图时，左上角贴边的框有时会引发预选框卡死。
如test26
加大最大子图数后解决了，但这不够好（先这样吧）
最大子图数改为5、6后，lprnet不工作。。。
# 问题4：FIFO读取冲突导致少数框下半部分被舍弃
FIFO读取应用当前box_cnt是否繁忙来控制，
*已解决*
*可以增加自动轮询操作。*
轮询操作可以解决多框中的少量闪烁，但会引起有的少框时的高频闪烁（多次尝试对相同子图缓存写入，导致crop丢帧）
box_cnt还是得尽量沿着一个方向自增，不做轮询了
# 问题5：有时候还是会有张冠李戴的bug。

# DDR同帧视频延迟链路进度（2026-06-28）

## 目标
原来的顶层 `hdmi_loop` 中，YOLO 检测和 overlay 画框同步运行。由于 YOLO 有固定流水延迟，overlay 使用的是后一帧视频，导致框和裁剪子图来自检测帧之后的视频帧。

本次目标是在 overlay/crop 前加入 DDR 视频缓存：输入视频连续写入 DDR，等 YOLO 第一张特征图第一次 `new_line_out_1_layer7` 输出时，启动从 DDR 读取当前检测对应的视频帧，使画框和 LPRNet 裁剪使用与 YOLO box 同一帧的底图。

## 已完成的结构改动
- 新增 `rtl_pds/ddr/ddr_video_delay_sync.sv`，作为视频流到 DDR 再读出的同步模块。
- 新增 `rtl_pds/ddr/ddr_wr_line_ram.sv`、`rtl_pds/ddr/ddr_rd_line_ram.sv`，用于像素时钟域和 DDR 时钟域之间的行级缓存。
- 迁移 DDR 控制相关 RTL：`wr_cmd_trans.v`、`wr_ctrl.v`、`rd_ctrl.v`、`wr_rd_ctrl_top.v`、`sync_vg.v`。
- 迁移 PDS DDR IP `ddr3_test`，工程内使用 `prj_pds/ipcore/ddr3_test` 作为 PDS 工程 IP 路径。
- `rtl_pds/hdmi_loop.sv` 增加 DDR3 顶层端口和 `ddr_init_done`，并例化 `ddr_video_delay_sync`。
- overlay/crop 的输入视频已经从 HDMI 原始输入切换为 DDR 读出的 `ddr_video_vs/hs/de/rgb`。
- DDR 读取触发信号由 `new_line_out_1_layer7` 的每帧第一次输出产生，通过 toggle 跨到 DDR 逻辑。

## 当前实现策略
- 外部视频为 RGB888，写入 DDR 时压缩为 RGB565，读出后再扩展回 RGB888。精度损失可接受，DDR 带宽和容量压力更低。
- 当前按 1280x720@60Hz 参数接入：`H_ACT=1280`、`V_ACT=720`、`H_TOTAL=1650`、`V_TOTAL=750`。
- DDR 地址划分为两个 frame bank。它不是等待整帧写完再读的传统高延迟帧缓存，而是用于避免跨帧地址覆盖；读启动仍由当前帧的 YOLO 流水线标志触发，以保持低延迟。
- 写入侧按行收集，16 个 RGB565 像素打包为一个 256-bit beat，再按行 burst 写 DDR。
- 读取侧按行 burst 读 DDR，使用双行 buffer 给像素时钟域连续输出，恢复 720p 视频时序。
- 设计假设 YOLO 流水延迟稳定且小于半帧，`new_line_out_1_layer7` 触发读取时不会读到尚未写入的行。

## PDS工程/IP迁移状态
- DDR IP 源文件现在放在 `prj_pds/ipcore/ddr3_test`，`hdmi_loop.pds` 中也使用 `ipcore/ddr3_test/...` 相对路径。
- 之前 `rtl_pds/ddr/ipcore/ddr3_test/rtl/ips2l_rst_sync_v1_3.vp` 读取失败，主要是 PDS 临时工程解析相对路径时不稳定；把 IP 放回 `prj_pds/ipcore` 并让工程直接引用该路径后，Design 文件树已经恢复。
- 用户确认当前 PDS 可以显示 Designs 文件树，compile 已通过。

## 仿真脚本
- `sim_pds/tb_ddr_video_delay_sync.sv`：DDR 视频延迟链路 testbench。
- `sim_pds/ddr3_test_axi_model.sv`：快速 AXI 行为模型，用于不等待真实 DDR 初始化的功能验证。
- `sim_pds/sim_modelsim/test_ddr_video_delay_sync.tcl`：快速功能仿真脚本。
- `sim_pds/sim_modelsim/10_Run_test_ddr_video_delay_sync.bat`：运行快速功能仿真。
- `sim_pds/sim_modelsim/test_ddr_video_delay_sync_pds_ip.tcl`：PDS DDR IP 联合仿真 smoke 脚本，主要检查编译、例化和短时间运行。
- `sim_pds/sim_modelsim/10a_Run_test_ddr_video_delay_sync_pds_ip.bat`：运行 PDS DDR IP smoke 仿真。
- `sim_pds/sim_modelsim/test_ddr_video_delay_sync_pds_ip_full.tcl` 和 `10b_Run_test_ddr_video_delay_sync_pds_ip_full.bat`：预留完整 PDS DDR 初始化联合仿真，运行时间会明显更长。

## 已验证
- 快速 AXI 行为模型仿真通过，验证 DDR 延迟链路的 RGB888 -> RGB565 -> RGB888 回读像素与预期一致。
- PDS DDR IP smoke 联合仿真脚本可用于检查 IP 编译/展开。
- PDS 工程 Designs 文件树恢复，compile 已通过。

## 后续待做
- 在真实板级约束和工程中继续检查 DDR 引脚、时钟、复位与 board 原理图是否完全一致。
- 跑完整综合/布局布线/时序，重点看 74.25MHz 像素域、DDR 用户时钟域和跨时钟握手。
- 上板验证 overlay 框位置、crop 子图和 LPRNet 输入是否确实来自同一帧。
- 如果发现读取追上写入，需要增加最小安全行延迟或基于写入行号的保护。
- 已完成初步上板测试，只看现象基本符合预期。

# LPRNet v10迁移进度（2026-07-01）

## 目标
将车牌识别网络从 `conv_data_lprnetv8_new_pds` 切换到 `conv_data_lprnetv10`。新网络仍只包含卷积层和最大池化层，YOLO 部分保持不变，LPRNet 继续复用从 layer20 开始的编号。

## 已完成的结构改动
- `python_script/CnnHardwareGenerator_pds.py` 已把 LPRNet 映射到 `conv_data_lprnetv10`。
- LPRNet v10 使用 layer20 到 layer31：
  - layer20、21、23、24、26、27、29、30、31 为卷积层。
  - layer22、25、28 为最大池化层。
  - layer31 为最终分类层，不接 ReLU。
- 已重新生成 layer20 到 layer31 的 header、weight、bias 和仿真输入 hex。
- 新增 layer29、layer30、layer31 相关 `*.vh`、`*.mem`、`*_input_9bit.hex`。
- `rtl_pds/data_process/layer.sv` 已补齐 layer29 到 layer31 的例化分支。
- `rtl_pds/top_lprnet.sv` 已改为 v10 的 layer20 到 layer31 串联结构，并使用 layer31 输出接 `lprnet_post_process`。
- `rtl_pds/hdmi_loop.sv` 中 LPRNet 输出宽度已切换为 layer31 参数。
- `crop_buffer_manager` 的 LPRNet 输入行节拍已按 layer24 参数计算；当前 80x20 输入下打印出的 `INPUT_LINE_GAP` 为 10240 cycles。

## 仿真脚本改动
- 测试入口统一使用 `sim_pds/sim_modelsim` 下的 `.bat` 文件。
- `2_Run_test_layer_run-all.bat` 支持指定 LPRNet 单层编号，例如：
  - `2_Run_test_layer_run-all.bat 20`
  - `2_Run_test_layer_run-all.bat 28`
  - `2_Run_test_layer_run-all.bat 31`
- `sim_pds/tb_layer.sv` 已改为参数化单层 testbench，支持 layer20 到 layer31。
- `python_script/compare_data_pds.py` 增加 `--layer` 参数，可对指定层输出与 MAT 参考数据做对比。
- `8_Run_test_lprnet_post_process.bat` 用于 layer31 加后处理的验证。
- `9b_Run_test_lprnet.bat` 用于完整 LPRNet v10 链路 smoke 仿真。
- 新增 `9c_Run_test_top_lprnet_syntax.bat`，用于 `top_lprnet` 语法/例化编译检查。

## 已验证
- 重新运行 `CnnHardwareGenerator_pds.py`，layer20 到 layer31 的参数、header、mem 和输入 hex 生成完成。
- `2_Run_test_layer_run-all.bat 20` 后执行 `compare_data_pds.py --layer 20`：
  - 已输出部分全部匹配，0 error。
  - 第二帧因仿真提前结束存在 incomplete frame warning，不是数据不匹配。
- `2_Run_test_layer_run-all.bat 28` 后执行 `compare_data_pds.py --layer 28`：
  - 0 error，PASS。
- `2_Run_test_layer_run-all.bat 30` 后执行 `compare_data_pds.py --layer 30`：
  - 0 error，PASS。
- `2_Run_test_layer_run-all.bat 31` 后执行 `compare_data_pds.py --layer 31`：
  - 0 error，PASS。
- `8_Run_test_lprnet_post_process.bat`：
  - 编译和仿真通过。
  - 当前参考输入下解码结果重复输出 `12 51 69 74 50 45 45`。
- `9b_Run_test_lprnet.bat`：
  - 完整 layer20 到 layer31 加后处理链路 smoke 仿真通过。
  - 当前打开中间层文件输出，只在后处理出现输出后提前结束，结果为 `post_frames=1`、`post_chars=2`。
  - 该测试用于确认完整链路能跑通，不等价于完整车牌字符串识别正确性验证。
- `9c_Run_test_top_lprnet_syntax.bat`：
  - `top_lprnet` 编译通过，0 error。

## 注意事项
- layer31 的硬件输出通道按 `CYCLE_PERIOD_OUT_LAYER31 * PE_COL_NUM_LAYER31` 打包，实际有效类别数为 76，blank 为 75；`compare_data_pds.py` 会跳过打包产生的无效补齐通道。
- 完整 LPRNet 仿真当前打开所有中间层输出，文件 IO 会明显拖慢 ModelSim；如只做快速 smoke test，可临时把 `CAPTURE_INTERMEDIATE` 改回 0。
- 后续若要做完整识别精度确认，应使用完整输入集跑 `9b_Run_test_lprnet.bat`，并结合后处理输出与软件参考结果逐帧对比。

## 全中间层文件 IO 验证与修复（2026-07-01）
- 按要求把 `sim_pds/tb_lprnet.sv` 中完整 LPRNet 链路的中间层文件 IO 打开，当前 `CAPTURE_INTERMEDIATE = 1`，会输出 layer20 到 layer31 的 `sim_out/layer*_output.hex`。
- `python_script/compare_data_pds.py` 已改为严格检查仿真输出；任何 `x`/`z` 都会立即报错，不再跳过未知态。
- 第一次打开全 IO 后，layer26 及之后出现 `x/z`。这里不能靠初始化 `sdp_ram` 掩盖，因为 BRAM 在仿真中故意不初始化，用来暴露逻辑是否读到了尚未写入的地址；后续应继续从 valid/读写时序上修正提前读问题。
- 在检查节拍后发现，layer26 及之后的数据不匹配主要来自生成器中两个最大池化层的输出节拍/通道数配置不对：
  - layer25 是最大池化层，应保持 layer24 的输出通道和输出节拍；已改为 `cycle_period_cout=layer24.cycle_period_cout`。
  - layer28 是最大池化层，应保持 layer27 的输出通道和输出节拍；已改为 `cycle_period_cout=layer27.cycle_period_cout`。
- 重新运行 `python_script/CnnHardwareGenerator_pds.py` 后，相关 layer header、weight/bias mem 和输入 hex 已刷新。

## 本轮验证结果
- `9b_Run_test_lprnet.bat`：
  - 完整 LPRNet v10 链路编译/仿真通过，0 error。
  - 打开全 IO 后仍需继续关注 `x/z`，因为 BRAM 保持不初始化；若再次出现 `x/z`，应优先排查对应层的 `new_line/data_valid` 和读写地址时序。
  - 对 layer20 到 layer31 逐层运行 `compare_data_pds.py --layer N`，当前输出的所有非未知态数据均 0 error、PASS。
  - 后几层会有 incomplete frame warning，原因是 smoke test 在后处理首次输出后提前结束；这不是数值不匹配。
- `2_Run_test_layer_run-all.bat 25` + `compare_data_pds.py --layer 25`：
  - 单层仿真 0 error，双帧输出完全匹配。
- `2_Run_test_layer_run-all.bat 28` + `compare_data_pds.py --layer 28`：
  - 单层仿真 0 error，双帧输出完全匹配。
- `8_Run_test_lprnet_post_process.bat`：
  - 后处理仿真 0 error，当前参考输入下输出序列重复为 `12 51 69 74 50 45 45`。
- `9c_Run_test_top_lprnet_syntax.bat`：
  - `top_lprnet` 编译通过，0 error。
- 注意：多个 ModelSim `.bat` 不能并行跑同一个 `rtl_work`，否则会出现 `_opt` 优化库锁导致的假失败；本轮有效验证均按顺序执行。
